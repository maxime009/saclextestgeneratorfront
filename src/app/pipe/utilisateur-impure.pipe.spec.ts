import { UtilisateurImpurePipe } from './utilisateur-impure.pipe';

describe('UtilisateurImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new UtilisateurImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
