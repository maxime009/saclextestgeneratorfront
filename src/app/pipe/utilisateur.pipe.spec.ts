import { UtilisateurPipe } from './utilisateur.pipe';

describe('UtilisateurPipe', () => {
  it('create an instance', () => {
    const pipe = new UtilisateurPipe();
    expect(pipe).toBeTruthy();
  });
});
