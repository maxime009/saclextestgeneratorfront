import {Utilisateur} from './utilisateur';

export interface Theme {
  id: string;
  libelle: string;
  description: string;
  dateCreation: string;
  dateModification: string;
  utilisateur: string;
}
